'use client';
import { useState } from 'react';
import Link from 'next/link';
import { Menu, X, ArrowUpRight } from 'lucide-react';
import { useAuth } from '@/context/AuthContext';
const Brand = () => <Link href="/" className="group flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[#375dfb] font-display text-sm font-bold text-white shadow-lg shadow-blue-500/25 transition-transform group-hover:-rotate-6">RB</span><span className="font-display text-lg font-bold tracking-tight text-slate-950">RoleBase<span className="text-[#375dfb]">.</span></span></Link>;
export default function Navbar() { const { isLoggedIn, isAdmin, logout } = useAuth(); const [open, setOpen] = useState(false); const links = [{ href: '/', label: 'Home' }, { href: '/about', label: 'About' }, ...(isAdmin ? [{ href: '/roles', label: 'Roles' }] : [])]; return <nav className="fixed inset-x-0 top-0 z-50 border-b border-slate-200/70 bg-white/80 backdrop-blur-xl"><div className="mx-auto flex h-[72px] max-w-7xl items-center justify-between px-5 sm:px-8"><Brand /><div className="hidden items-center gap-8 md:flex">{links.map((link) => <Link key={link.href} href={link.href} className="text-sm font-semibold text-slate-500 transition-colors hover:text-slate-950">{link.label}</Link>)}{isLoggedIn ? <button onClick={logout} className="rounded-full bg-slate-950 px-5 py-2.5 text-sm font-bold text-white transition hover:bg-[#375dfb]">Log out</button> : <Link href="/login" className="inline-flex items-center gap-1 rounded-full bg-slate-950 px-5 py-2.5 text-sm font-bold text-white transition hover:bg-[#375dfb]">Get started <ArrowUpRight className="h-4 w-4" /></Link>}</div><button onClick={() => setOpen(!open)} aria-label="Toggle menu" className="grid h-10 w-10 place-items-center rounded-full border border-slate-200 text-slate-950 md:hidden">{open ? <X size={19} /> : <Menu size={20} />}</button></div>{open && <div className="border-t border-slate-100 bg-white px-5 pb-5 md:hidden"><div className="flex flex-col gap-1 pt-3">{links.map((link) => <Link onClick={() => setOpen(false)} key={link.href} href={link.href} className="rounded-xl px-4 py-3 text-sm font-bold text-slate-700 hover:bg-slate-50">{link.label}</Link>)}{isLoggedIn ? <button onClick={logout} className="mt-2 rounded-xl bg-slate-950 px-4 py-3 text-left text-sm font-bold text-white">Log out</button> : <Link onClick={() => setOpen(false)} href="/login" className="mt-2 rounded-xl bg-[#375dfb] px-4 py-3 text-sm font-bold text-white">Get started</Link>}</div></div>}</nav>; }
